# 저장소 루트 무시 목록
#
# 하위 프로젝트는 자체 규칙을 둔다. 예: landing/.gitignore
# 여기에는 저장소 전역에 적용할 것만 좁게 지정한다.

# AI 코딩 도구의 세션·상태 디렉터리
# 기계별 상태이므로 공유하지 않는다.
.omc/
.codex-tmp/
.gstack/

# 환경 파일과 로컬 시크릿
# 실제 값은 어떤 경우에도 커밋하지 않는다.
.env
.env.*
.dev.vars
.dev.vars.*

# OS·편집기 산출물
.DS_Store
Thumbs.db
*.swp
