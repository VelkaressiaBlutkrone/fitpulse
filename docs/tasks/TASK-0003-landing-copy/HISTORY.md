# TASK-0003 History

| Date | Type | ID | Status | Branch | Commit | PR | Validation | Description |
|---|---|---|---|---|---|---|---|---|
| 2026-08-17 | Task | TASK-0003 | In Progress | task/TASK-0003-landing-copy | - | - | 문서 검토 | TASK 생성. TASK-0001에서 제외한 카피 작업을 분리 |
| 2026-08-17 | Workflow | WF-01 | Review | task/TASK-0003-landing-copy | - | - | 검증 5건 Passed, 1건 Not Run | 카피 재정리 완료 |

## 2026-08-17 — 작업 경위

### 선행 정리

같은 커밋에서 TASK-0002를 `Done`으로 마감했다. PR #10 병합은 끝났으나 병합 확인이 병합 후에만 가능해 문서가 `Review`로 남아 있었다.

### 변경한 카피

실행 계획 19~21행이 확정한 **메인 문구·보조 문구·CTA는 건드리지 않았다.** 25~33행의 섹션 구성도 그대로다. 그 안에서 표현만 다듬었다.

| 대상 | 변경 |
|---|---|
| 확인 단계 고지 | **신설.** 폼 위에 "신청은 두 단계입니다"와 14일 만료를 명시 |
| 신뢰 목록 | "확인된 주소만 대기자로 집계" 추가 |
| 문제 서사 3개 | 구체화. 예: "증량 기준이 모호합니다" → "5회 목표를 4회에 멈췄을 때 같은 무게로 갈지, 2.5kg을 올릴지" |
| 제안 흐름 3개 | 3단계를 "보여줄 계획입니다"로 바꿔 미구현임을 명확히 |
| `signal-row` | "출력" → "보여줄 것". 이미 동작하는 기능처럼 읽히지 않게 |
| FAQ | 4개 → 7개. 확인 절차, 저장 위치, 삭제 잔존 추가 |

### 확인 단계 고지를 넣은 이유

TASK-0001 WF-03이 더블 옵트인을 도입해 **등록만으로는 신청이 완료되지 않는다.** 그런데 제출 전에는 그 사실을 알리지 않아, 메일을 확인하지 않은 사용자가 14일 뒤 삭제되고 집계에도 빠진다.

이것은 취향 문제가 아니라 **명백한 이탈 요인**이므로 이 TASK의 핵심으로 다뤘다.

### AC-07을 검증하지 못했다

FAQ에 넣은 "저장 국가 특정 불가"와 "최대 30일 잔존"은 WF-07에서 확인한 사실이다. 그러나 같은 사실을 담은 개인정보 안내 수정은 **TASK-0001 WF-04에 있고 `develop`에 병합되지 않았다.**

이 TASK는 `develop`에서 분기했으므로 현재 브랜치의 안내는 이전 버전이다. **FAQ가 안내보다 최신 사실을 말하는 상태**이며, FAQ가 틀린 것이 아니라 안내가 낡았다.

두 문구를 빼는 대신 남겼다. 등록 여부를 정할 때 알아야 하는 정보이고, 빼면 감추는 쪽이 되기 때문이다. FAQ에 안내 링크를 걸어 정본을 가리킨다.

**AC-07은 TASK-0001이 `develop`에 병합되는 시점에 검증한다.** 그때까지 미검증으로 둔다.

### 개선 효과를 주장하지 않는다

랜딩은 소유자 전용이고 외부 방문자가 0명이다. 이 변경이 전환을 올린다고 **주장할 수 없다.** 전환율 비교는 공개 후 기준선을 확보한 뒤에만 가능하다(TASK-0001 WF-06).

이 작업은 "명백한 이탈 요인 제거"와 "사실 정확성 향상"에 한정한다.

### 검증 결과

| 항목 | 결과 |
|---|---|
| `cd landing && npm test` | tests 8, pass 8, fail 0 |
| `cd landing && npm run lint` | 오류 0 |
| 실행 계획 확정 값(메인·보조·CTA) | 3개 모두 유지 |
| 섹션 구성 | 5개 유지 |
| `git diff --check` | 경고 0건 |
| FAQ와 개인정보 안내 대조 | **Not Run — 시차** |

기존 테스트가 효능 주장(`의학적으로 안전|부상을 예방|회복을 보장`)을 금지하고 있고 통과했다. 여기에 "지금 바로 추천받|추천을 받아보세요|자동으로 조절해 드립니다" 금지를 추가해 기능 과장 표현을 더 막았다.

`develop` 기준이라 테스트 수가 8개다. TASK-0001의 신규 테스트 13개는 그 TASK에 있다.

### 2026-08-17 — `develop` 최신화와 AC-07 해소

TASK-0001이 PR #14로 `develop`에 병합된 뒤(`d69a995`) 이 Branch에 `develop`을 병합했다(`a9059ba`).

**병합 결과**

Conflict 0건. `landing/app/page.tsx`를 양쪽이 함께 건드렸으나 git이 자동 병합했다. **자동 병합이라 조용히 한쪽 의도가 사라질 수 있어 직접 검수했고**, 두 의도가 모두 보존된 것을 확인했다.

| 출처 | 보존된 변경 |
|---|---|
| TASK-0003 | 확인 단계 사전 고지 문단, `확인된 주소만 대기자로 집계` 항목, FAQ 7개 |
| TASK-0001 | `import { env }`, `turnstileSiteKey()`, `<WaitlistForm turnstileSiteKey={...} />` |

`turnstileSiteKey` prop이 사라졌다면 남용 방어 위젯이 조용히 렌더링되지 않았을 것이다. 텍스트 충돌이 없다고 해서 검수를 생략하면 안 되는 사례다.

**PR #11 CI 복구**

최신화 전 PR #11은 `npm ci`에서 실패하고 있었다(`Missing: @emnapi/runtime@1.11.3`). 원인은 이 Branch의 변경이 아니라 `develop`의 lock 결손이었다. `develop` 병합 후 Landing CI `verify`가 **Passed (1m1s)**로 바뀌어 원인이 실증되었다.

**AC-07 검증과 정정**

병합으로 개인정보 안내가 같은 트리에 들어와 드디어 대조가 가능해졌다. 예상은 "자동 해소"였으나 **3건 중 2건만 해소되었다.**

저장 국가 문구와 삭제 후 30일 잔존 문구는 안내와 일치했다. 그러나 확인 단계 고지의 "확인하지 않은 신청은 같은 기간이 지나면 **삭제됩니다**"가 안내의 "**삭제 대상이 됩니다**"보다 강한 약속이었다. 안내는 배포 환경의 cron 미지원 때문에 만료 삭제가 지연될 수 있다고 유보하는데, 랜딩만 단정했다.

소유자 결정에 따라 랜딩 문구를 안내에 맞췄고, 회귀 방지 단언 2개를 테스트에 추가했다. **Test-First로 진행했다** — 단언만 먼저 넣고 실행해 `pass 20 / fail 1` 실패를 확인한 뒤 문구를 고쳐 `pass 21 / fail 0`을 확인했다.

**검증 (병합 + 정정 후)**

| 항목 | 결과 |
|---|---|
| `npm ci` | 507 packages, 오류 없음 |
| `npm test` | `tests 21 / pass 21 / fail 0` |
| `npm run lint` | 오류 0 |
| `npm audit --omit=dev --audit-level=high` | `found 0 vulnerabilities` |
| `page.tsx` 자동 병합 검수 | 양쪽 의도 보존 확인 |
| AC-07 대조 | 정정 후 모순 0건 |

**남은 한계**

추가한 테스트는 랜딩 문구가 유보 표현인지만 강제한다. 랜딩과 개인정보 안내를 **서로** 대조하지는 않으므로, 안내 쪽이 바뀌면 다시 어긋날 수 있다. 두 파일을 함께 읽는 대조 테스트는 이 TASK의 범위를 넘어 추가하지 않았다.

실행 환경은 Windows / Node.js v24.12.0이며 CI는 Node.js 22다. 런타임이 동일하지 않으므로 확정은 PR #11의 Landing CI 결과로 한다.
